services:
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_DATABASE: "dev_book"
      MYSQL_USER: "root"
      MYSQL_PASSWORD: "root"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: